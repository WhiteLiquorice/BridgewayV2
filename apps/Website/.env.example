# Supabase (use PUBLIC_ prefix for Astro — these are safe to expose)
PUBLIC_SUPABASE_URL=https://your-project.supabase.co
PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Stripe price ID (safe to expose — it's a public identifier)
PUBLIC_STRIPE_PRICE_ID=price_xxxxx
